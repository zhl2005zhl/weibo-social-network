spring:
  profiles:
    active: dev
  application:
    name: weibo-backend

# JWT配置
jwt:
  secret: weibo_secret_key
  expiration: 86400000  # 24小时

# 分页配置
page:
  size: 10
  max-size: 100

# 文件上传配置
file:
  upload-dir: ./uploads
  base-url: http://localhost:8080/api/files